{
  "name": "alfa-cbdc-hub-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Playwright end-to-end suite for Alfa Global CBDC Hub, run against an already-running stand (SPA + BFF + Backend services).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "install-browsers": "playwright install --with-deps chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.47.0",
    "@types/node": "^22.7.0",
    "typescript": "^5.5.3"
  }
}
